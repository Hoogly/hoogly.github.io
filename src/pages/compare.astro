---
import Layout from "../layouts/Layout.astro";
---

<Layout
    title="Stop Measuring Engagement. Start Listening to Your People | Hoogly.ai"
    description="Legacy survey tools create a 'feedback-without-action' loop that breaks trust. Hoogly is a Culture OS designed for one thing: turning continuous feedback into immediate, meaningful action."
    type="website"
    bodyClass=""
>
    <!-- Hero Section: Capture the Pain & Introduce the New Category -->
    <section class="hero-section">
        <div class="container">
            <div class="hero-content">
                <div class="hero-text">
                    <h1 class="hero-title scroll-animate">
                        Stop Measuring Engagement.<br />
                        <span class="hero-highlight">
                            <span class="hero-highlight-text">Start Listening</span>
                        </span> to Your People.
                    </h1>
                    <p class="hero-subtitle scroll-animate">
                        Legacy survey tools create a "feedback-without-action" loop that breaks trust. 
                        Hoogly is a Culture OS designed for one thing: turning continuous feedback 
                        into immediate, meaningful action.
                    </p>
                    <div class="hero-cta scroll-animate">
                        <a href="#how-hoogly-different" class="btn btn-primary">See How It Works</a>
                        <a href="#demo-form" class="btn btn-secondary">Book a Demo</a>
                    </div>
                </div>
                <div class="hero-visual scroll-animate">
                    <div class="visual-container">
                        <div class="messy-line"></div>
                        <div class="clean-line"></div>
                        <div class="transformation-arrow">→</div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Problem Section: Agitate the Pain of Legacy Tools -->
    <section class="problem-section">
        <div class="container">
            <h2 class="section-title scroll-animate">Are You Stuck in the Survey-to-Nowhere Cycle?</h2>
            <div class="problem-grid">
                <div class="problem-item scroll-animate">
                    <div class="problem-icon">📊</div>
                    <h3>Survey Fatigue</h3>
                    <p>Employees give low-effort, low-context answers because they've lost faith in the process.</p>
                </div>
                <div class="problem-item scroll-animate">
                    <div class="problem-icon">📈</div>
                    <h3>Inactionable Data</h3>
                    <p>You get spreadsheets of numbers, not a clear story of what's really happening.</p>
                </div>
                <div class="problem-item scroll-animate">
                    <div class="problem-icon">⏰</div>
                    <h3>Months-Long Delays</h3>
                    <p>The typical "action plan" process takes 6 months, by which time the feedback is stale.</p>
                </div>
                <div class="problem-item scroll-animate">
                    <div class="problem-icon">💔</div>
                    <h3>Broken Trust</h3>
                    <p>Employees see no change, stop giving honest feedback, and your best people leave.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Solution Section: Introduce the Hoogly Philosophy -->
    <section class="solution-section" id="how-hoogly-different">
        <div class="container">
            <h2 class="section-title scroll-animate">The Shift From Measuring to a Culture Operating System</h2>
            <div class="solution-steps">
                <div class="solution-step scroll-animate">
                    <div class="step-number">1</div>
                    <div class="step-content">
                        <h3>Listen</h3>
                        <p>Go beyond scores with continuous, anonymous, AI-powered conversations that reveal the 'why' behind the 'what'.</p>
                    </div>
                </div>
                <div class="solution-step scroll-animate">
                    <div class="step-number">2</div>
                    <div class="step-content">
                        <h3>Understand</h3>
                        <p>Get AI-surfaced themes and insights in real-time, so you know exactly where to focus your energy.</p>
                    </div>
                </div>
                <div class="solution-step scroll-animate">
                    <div class="step-number">3</div>
                    <div class="step-content">
                        <h3>Act</h3>
                        <p>Reduce time-to-action from 6 months to 1 week with actionable nudges and progress tracking that shows your team you're serious about change.</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Comparison Table: Reimagined for Impact -->
    <section class="comparison-section">
        <div class="container">
            <h2 class="section-title scroll-animate">How Hoogly Compares to Traditional Tools</h2>
            <div class="comparison-table">
                <div class="table-header">
                    <div class="empty-cell"></div>
                    <div class="product-col hoogly-col">Hoogly.ai</div>
                    <div class="product-col">Culture Amp</div>
                    <div class="product-col">Officevibe</div>
                    <div class="product-col">BambooHR</div>
                </div>
                <div class="table-row">
                    <div class="feature-label">Core Philosophy</div>
                    <div class="product-col">✅ Always-on 'Listening'</div>
                    <div class="product-col">❌ Periodic 'Measuring'</div>
                    <div class="product-col">❌ Pulse 'Measuring'</div>
                    <div class="product-col">❌ System of Record</div>
                </div>
                <div class="table-row">
                    <div class="feature-label">Primary Goal</div>
                    <div class="product-col">Building Culture & Trust</div>
                    <div class="product-col">Engagement & Performance Scores</div>
                    <div class="product-col">Manager Feedback</div>
                    <div class="product-col">HR Admin & Payroll</div>
                </div>
                <div class="table-row">
                    <div class="feature-label">Time-to-Action</div>
                    <div class="product-col">✅ 1 Week</div>
                    <div class="product-col">❌ 3-6 Months</div>
                    <div class="product-col">❌ 1-3 Months</div>
                    <div class="product-col">❌ N/A</div>
                </div>
                <div class="table-row">
                    <div class="feature-label">Insight Quality</div>
                    <div class="product-col">✅ Qualitative & Contextual</div>
                    <div class="product-col">❌ Primarily Quantitative</div>
                    <div class="product-col">❌ Repetitive & Quantitative</div>
                    <div class="product-col">❌ HR Admin Data</div>
                </div>
                <div class="table-row">
                    <div class="feature-label">Anonymity & Trust</div>
                    <div class="product-col">✅ Guaranteed Foundational</div>
                    <div class="product-col">⚠️ Common De-anonymization Concerns</div>
                    <div class="product-col">⚠️ Employee Trust Issues</div>
                    <div class="product-col">❌ N/A</div>
                </div>
                <div class="table-row">
                    <div class="feature-label">Ideal Company Size</div>
                    <div class="product-col">✅ 20-200 Scale-ups</div>
                    <div class="product-col">❌ Enterprise (250+)</div>
                    <div class="product-col">❌ SMBs / Teams</div>
                    <div class="product-col">❌ Mid-Market</div>
                </div>
            </div>
        </div>
    </section>

    <!-- Feature Blocks: Go Deeper -->
    <section class="features-section">
        <div class="container">
            <h2 class="section-title scroll-animate">3 Ways Hoogly Delivers Where Others Can't</h2>
            
            <div class="feature-block scroll-animate">
                <div class="feature-content">
                    <h3>From Numbers to Narratives</h3>
                    <h4>Go Beyond Scores with AI-Powered Conversations</h4>
                    <p>While other tools give you spreadsheets of scores, Hoogly reveals the real stories behind your culture. Our AI-powered conversations surface themes like "Concerns about Q4 Engineering Roadmap" with full context, so you know exactly what to address.</p>
                    <div class="feature-visual">
                        <div class="mockup-container">
                            <div class="conversation-thread">
                                <div class="message">"I'm worried about the new project timeline..."</div>
                                <div class="message">"The team seems stressed about deadlines"</div>
                                <div class="insight">💡 <strong>Theme:</strong> Q4 Engineering Roadmap Concerns</div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="feature-block scroll-animate">
                <div class="feature-content">
                    <h3>From Delay to Decisiveness</h3>
                    <h4>Act in a Week, Not a Quarter</h4>
                    <p>Traditional tools take months to turn insights into action. Hoogly gives you actionable nudges within days, with progress tracking that shows your team their feedback leads to real change.</p>
                    <div class="feature-visual">
                        <div class="mockup-container">
                            <div class="action-dashboard">
                                <div class="action-item">
                                    <span class="action-title">Address Q4 concerns</span>
                                    <span class="action-status">✅ Completed</span>
                                </div>
                                <div class="action-item">
                                    <span class="action-title">Schedule team check-in</span>
                                    <span class="action-status">🔄 In Progress</span>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="feature-block scroll-animate">
                <div class="feature-content">
                    <h3>From Fear to Frankness</h3>
                    <h4>Built on a Foundation of Guaranteed Anonymity</h4>
                    <p>Trust is everything in culture work. Unlike other platforms where employees worry about de-anonymization, Hoogly's architecture guarantees true anonymity, encouraging honest, unfiltered feedback.</p>
                    <div class="feature-visual">
                        <div class="mockup-container">
                            <div class="privacy-shield">
                                <div class="shield-icon">🛡️</div>
                                <div class="privacy-text">Guaranteed Anonymity</div>
                                <div class="privacy-subtext">Architecture-level protection</div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Social Proof Section -->
    <section class="social-proof-section">
        <div class="container">
            <h2 class="section-title scroll-animate">Designed for the Modern Head of People</h2>
            
            <div class="founder-quote scroll-animate">
                <blockquote>
                    "As a founder, I saw firsthand how traditional surveys created more problems than they solved. 
                    We built Hoogly because People leaders at fast-growing companies don't need another dashboard; 
                    they need a direct line to the truth and the tools to act on it instantly."
                </blockquote>
                <cite>— Hoogly Team</cite>
            </div>

            <div class="testimonial scroll-animate">
                <blockquote>
                    "Hoogly is a complete game changer, when it comes to employee engagement"
                </blockquote>
                <cite>— Hassanah Rudd, CPO at Hark Medical<br />Ex Global Head of Workforce Planning Canva</cite>
            </div>

            <div class="team-credentials scroll-animate">
                <h3>Our team has built products and culture at:</h3>
                <div class="company-logos">
                    <div class="logo-item">Canva</div>
                    <div class="logo-item">Airbnb</div>
                    <div class="logo-item">Meta</div>
                    <div class="logo-item">TikTok</div>
                    <div class="logo-item">Oracle</div>
                </div>
            </div>
        </div>
    </section>

    <!-- Final CTA Section -->
    <section class="final-cta-section" id="demo-form">
        <div class="container">
            <h2 class="cta-title scroll-animate">Ready to See What Your Team is *Really* Thinking?</h2>
            <p class="cta-subtitle scroll-animate">
                Stop chasing survey scores. A 15-minute demo of Hoogly will show you how to get the 
                actionable, qualitative insights you need to build a culture that retains top talent.
            </p>
            
            <div class="demo-form-container scroll-animate">
                <form class="demo-form" id="demo-request-form">
                    <div class="form-row">
                        <div class="form-group">
                            <label for="name">Full Name</label>
                            <input type="text" id="name" name="name" required />
                        </div>
                        <div class="form-group">
                            <label for="email">Work Email</label>
                            <input type="email" id="email" name="email" required />
                        </div>
                    </div>
                    <div class="form-row">
                        <div class="form-group">
                            <label for="company">Company</label>
                            <input type="text" id="company" name="company" required />
                        </div>
                        <div class="form-group">
                            <label for="company-size">Company Size</label>
                            <select id="company-size" name="company-size" required>
                                <option value="">Select size</option>
                                <option value="20-50">20-50 employees</option>
                                <option value="51-100">51-100 employees</option>
                                <option value="101-200">101-200 employees</option>
                                <option value="200+">200+ employees</option>
                            </select>
                        </div>
                    </div>
                    <button type="submit" class="btn btn-primary btn-large">Book Your Demo</button>
                </form>
            </div>
        </div>
    </section>
</Layout>

<style>
    /* Hero Section */
    .hero-section {
        padding: var(--space-20) 0 var(--space-16) 0;
        background: linear-gradient(135deg, #F6F4FE 0%, #F5E9FA 100%);
        min-height: 80vh;
        display: flex;
        align-items: center;
    }

    .hero-content {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: var(--space-16);
        align-items: center;
    }

    .hero-title {
        font-family: 'Outfit', sans-serif;
        font-size: 4rem;
        font-weight: 400;
        line-height: 1.1;
        margin-bottom: var(--space-6);
        color: var(--color-neutral-900);
    }

    .hero-subtitle {
        font-size: 1.5rem;
        color: var(--color-neutral-700);
        line-height: 1.6;
        margin-bottom: var(--space-8);
    }

    .hero-cta {
        display: flex;
        gap: var(--space-4);
        flex-wrap: wrap;
    }

    .hero-visual {
        display: flex;
        justify-content: center;
        align-items: center;
    }

    .visual-container {
        position: relative;
        width: 300px;
        height: 200px;
    }

    .messy-line {
        position: absolute;
        top: 50px;
        left: 0;
        width: 100%;
        height: 4px;
        background: linear-gradient(90deg, #ff6b6b, #4ecdc4, #45b7d1, #96ceb4);
        border-radius: 2px;
        opacity: 0.7;
        animation: messyWave 3s ease-in-out infinite;
    }

    .clean-line {
        position: absolute;
        top: 100px;
        left: 0;
        width: 0%;
        height: 4px;
        background: #10b981;
        border-radius: 2px;
        animation: cleanGrow 2s ease-out 1s forwards;
    }

    .transformation-arrow {
        position: absolute;
        top: 75px;
        right: -20px;
        font-size: 2rem;
        color: var(--color-purple);
        animation: bounce 2s ease-in-out infinite;
    }

    @keyframes messyWave {
        0%, 100% { transform: translateY(0) scaleX(1); }
        50% { transform: translateY(-10px) scaleX(1.1); }
    }

    @keyframes cleanGrow {
        0% { width: 0%; }
        100% { width: 100%; }
    }

    @keyframes bounce {
        0%, 100% { transform: translateX(0); }
        50% { transform: translateX(10px); }
    }

    /* Problem Section */
    .problem-section {
        padding: var(--space-20) 0;
        background: white;
    }

    .problem-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: var(--space-8);
        margin-top: var(--space-12);
    }

    .problem-item {
        text-align: center;
        padding: var(--space-8);
        border-radius: var(--radius-xl);
        background: var(--color-neutral-50);
        transition: transform 0.2s ease;
    }

    .problem-item:hover {
        transform: translateY(-4px);
    }

    .problem-icon {
        font-size: 3rem;
        margin-bottom: var(--space-4);
    }

    .problem-item h3 {
        font-size: var(--font-size-xl);
        color: var(--color-neutral-900);
        margin-bottom: var(--space-3);
    }

    .problem-item p {
        color: var(--color-neutral-700);
        line-height: 1.6;
    }

    /* Solution Section */
    .solution-section {
        padding: var(--space-20) 0;
        background: var(--color-neutral-50);
    }

    .solution-steps {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: var(--space-8);
        margin-top: var(--space-12);
    }

    .solution-step {
        display: flex;
        gap: var(--space-6);
        align-items: flex-start;
    }

    .step-number {
        width: 60px;
        height: 60px;
        border-radius: 50%;
        background: var(--color-purple);
        color: white;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: var(--font-size-xl);
        font-weight: bold;
        flex-shrink: 0;
    }

    .step-content h3 {
        font-size: var(--font-size-2xl);
        color: var(--color-neutral-900);
        margin-bottom: var(--space-3);
    }

    .step-content p {
        color: var(--color-neutral-700);
        line-height: 1.6;
    }

    /* Comparison Section */
    .comparison-section {
        padding: var(--space-20) 0;
        background: white;
    }

    .comparison-table {
        background: white;
        border-radius: var(--radius-2xl);
        box-shadow: var(--shadow-lg);
        overflow: hidden;
        width: 100%;
        margin-top: var(--space-12);
    }

    .table-header {
        display: grid;
        grid-template-columns: 220px repeat(4, 1fr);
        background: var(--color-neutral-50);
        border-bottom: 1px solid var(--color-neutral-200);
    }

    .table-row {
        display: grid;
        grid-template-columns: 220px repeat(4, 1fr);
        border-bottom: 1px solid var(--color-neutral-100);
        align-items: center;
    }

    .table-row:last-child {
        border-bottom: none;
    }

    .feature-label {
        font-weight: 600;
        color: var(--color-neutral-800);
        padding: var(--space-6);
        border-right: 1px solid var(--color-neutral-100);
        background: var(--color-neutral-50);
    }

    .empty-cell {
        padding: var(--space-6);
        border-right: 1px solid var(--color-neutral-100);
    }

    .product-col {
        padding: var(--space-6);
        font-weight: 500;
        border-right: 1px solid var(--color-neutral-100);
        display: flex;
        align-items: center;
    }

    .product-col:last-child {
        border-right: none;
    }

    .hoogly-col {
        background: #FFF0F7;
        font-weight: 600;
        color: var(--color-neutral-900);
        position: relative;
        box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
    }

    /* Features Section */
    .features-section {
        padding: var(--space-20) 0;
        background: var(--color-neutral-50);
    }

    .feature-block {
        margin-bottom: var(--space-16);
        background: white;
        border-radius: var(--radius-2xl);
        padding: var(--space-12);
        box-shadow: var(--shadow-lg);
    }

    .feature-content h3 {
        font-size: var(--font-size-3xl);
        color: var(--color-neutral-900);
        margin-bottom: var(--space-2);
    }

    .feature-content h4 {
        font-size: var(--font-size-xl);
        color: var(--color-purple);
        margin-bottom: var(--space-4);
    }

    .feature-content p {
        font-size: var(--font-size-lg);
        color: var(--color-neutral-700);
        line-height: 1.6;
        margin-bottom: var(--space-8);
    }

    .feature-visual {
        margin-top: var(--space-8);
    }

    .mockup-container {
        background: var(--color-neutral-50);
        border-radius: var(--radius-lg);
        padding: var(--space-6);
        border: 1px solid var(--color-neutral-200);
    }

    .conversation-thread {
        display: flex;
        flex-direction: column;
        gap: var(--space-3);
    }

    .message {
        background: white;
        padding: var(--space-3) var(--space-4);
        border-radius: var(--radius-md);
        border-left: 3px solid var(--color-purple);
    }

    .insight {
        background: #f0f9ff;
        padding: var(--space-3) var(--space-4);
        border-radius: var(--radius-md);
        border-left: 3px solid #0ea5e9;
    }

    .action-dashboard {
        display: flex;
        flex-direction: column;
        gap: var(--space-3);
    }

    .action-item {
        display: flex;
        justify-content: space-between;
        align-items: center;
        background: white;
        padding: var(--space-3) var(--space-4);
        border-radius: var(--radius-md);
        border: 1px solid var(--color-neutral-200);
    }

    .privacy-shield {
        text-align: center;
        padding: var(--space-8);
    }

    .shield-icon {
        font-size: 4rem;
        margin-bottom: var(--space-4);
    }

    .privacy-text {
        font-size: var(--font-size-xl);
        font-weight: 600;
        color: var(--color-neutral-900);
        margin-bottom: var(--space-2);
    }

    .privacy-subtext {
        color: var(--color-neutral-600);
    }

    /* Social Proof Section */
    .social-proof-section {
        padding: var(--space-20) 0;
        background: white;
    }

    .founder-quote, .testimonial {
        text-align: center;
        margin-bottom: var(--space-12);
    }

    .founder-quote blockquote, .testimonial blockquote {
        font-size: var(--font-size-xl);
        color: var(--color-neutral-800);
        line-height: 1.6;
        margin-bottom: var(--space-4);
        font-style: italic;
    }

    .founder-quote cite, .testimonial cite {
        color: var(--color-neutral-600);
        font-size: var(--font-size-lg);
    }

    .team-credentials {
        text-align: center;
    }

    .team-credentials h3 {
        font-size: var(--font-size-xl);
        color: var(--color-neutral-900);
        margin-bottom: var(--space-6);
    }

    .company-logos {
        display: flex;
        justify-content: center;
        gap: var(--space-8);
        flex-wrap: wrap;
    }

    .logo-item {
        background: var(--color-neutral-100);
        padding: var(--space-3) var(--space-6);
        border-radius: var(--radius-lg);
        font-weight: 500;
        color: var(--color-neutral-700);
    }

    /* Final CTA Section */
    .final-cta-section {
        padding: var(--space-20) 0;
        background: linear-gradient(135deg, #F7F3F0 0%, #F0E6FF 100%);
        text-align: center;
    }

    .cta-title {
        font-family: 'Outfit', sans-serif;
        font-size: 3rem;
        font-weight: 400;
        color: var(--color-neutral-900);
        margin-bottom: var(--space-4);
    }

    .cta-subtitle {
        font-size: var(--font-size-xl);
        color: var(--color-neutral-700);
        margin-bottom: var(--space-12);
        max-width: 600px;
        margin-left: auto;
        margin-right: auto;
    }

    .demo-form-container {
        max-width: 600px;
        margin: 0 auto;
        background: white;
        border-radius: var(--radius-2xl);
        padding: var(--space-12);
        box-shadow: var(--shadow-xl);
    }

    .demo-form {
        display: flex;
        flex-direction: column;
        gap: var(--space-6);
    }

    .form-row {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: var(--space-4);
    }

    .form-group {
        display: flex;
        flex-direction: column;
        gap: var(--space-2);
    }

    .form-group label {
        font-weight: 500;
        color: var(--color-neutral-900);
    }

    .form-group input,
    .form-group select {
        padding: var(--space-3);
        border: 1px solid var(--color-neutral-200);
        border-radius: var(--radius-lg);
        font-size: var(--font-size-base);
        transition: border-color 0.2s;
    }

    .form-group input:focus,
    .form-group select:focus {
        outline: none;
        border-color: var(--color-purple);
    }

    .btn {
        display: inline-block;
        padding: var(--space-3) var(--space-6);
        border-radius: var(--radius-lg);
        text-decoration: none;
        font-weight: 500;
        transition: all 0.2s;
        border: none;
        cursor: pointer;
        font-size: var(--font-size-base);
    }

    .btn-primary {
        background: var(--color-purple);
        color: white;
    }

    .btn-primary:hover {
        background: var(--color-purple-dark);
        transform: translateY(-2px);
    }

    .btn-secondary {
        background: white;
        color: var(--color-purple);
        border: 2px solid var(--color-purple);
    }

    .btn-secondary:hover {
        background: var(--color-purple);
        color: white;
    }

    .btn-large {
        padding: var(--space-4) var(--space-8);
        font-size: var(--font-size-lg);
        width: 100%;
    }

    /* Section Titles */
    .section-title {
        font-family: 'Outfit', sans-serif;
        font-size: 3rem;
        font-weight: 400;
        text-align: center;
        color: var(--color-neutral-900);
        margin-bottom: var(--space-8);
    }

    /* Hero Highlight */
    .hero-highlight {
        position: relative;
        color: var(--color-neutral-900);
        font-weight: 600;
        padding: 0.08em 0.5em;
        border-radius: 2em;
        display: inline-block;
        overflow: visible;
        background: none;
        z-index: 1;
        vertical-align: middle;
    }

    .hero-highlight::before {
        content: '';
        position: absolute;
        left: 0;
        top: 0;
        height: 100%;
        width: 0%;
        background: rgba(255, 125, 97, 0.5);
        border-radius: 2em;
        z-index: 0;
        transition: width 1.2s cubic-bezier(0.77, 0, 0.175, 1);
    }

    .hero-highlight.animated::before {
        width: 100%;
    }

    /* Scroll Animation */
    .scroll-animate {
        opacity: 0;
        transform: translateY(30px);
        transition: opacity 0.8s ease-out, transform 0.8s ease-out;
    }

    .scroll-animate.animated {
        opacity: 1;
        transform: translateY(0);
    }

    /* Responsive Design */
    @media (max-width: 1024px) {
        .hero-content {
            grid-template-columns: 1fr;
            gap: var(--space-12);
        }

        .hero-title {
            font-size: 3rem;
        }

        .solution-steps {
            grid-template-columns: 1fr;
        }

        .form-row {
            grid-template-columns: 1fr;
        }
    }

    @media (max-width: 768px) {
        .hero-title {
            font-size: 2.5rem;
        }

        .hero-subtitle {
            font-size: 1.25rem;
        }

        .section-title {
            font-size: 2rem;
        }

        .cta-title {
            font-size: 2rem;
        }

        .table-header, .table-row {
            grid-template-columns: 150px repeat(2, 1fr);
            font-size: 0.9rem;
        }

        .product-col:nth-child(n+4) {
            display: none;
        }

        .company-logos {
            gap: var(--space-4);
        }

        .logo-item {
            padding: var(--space-2) var(--space-4);
            font-size: var(--font-size-sm);
        }
    }

    @media (max-width: 480px) {
        .hero-title {
            font-size: 2rem;
        }

        .hero-cta {
            flex-direction: column;
        }

        .btn {
            width: 100%;
            text-align: center;
        }

        .table-header, .table-row {
            grid-template-columns: 120px 1fr;
            font-size: 0.85rem;
        }

        .product-col:nth-child(n+3) {
            display: none;
        }
    }
</style>

<script>
    document.addEventListener("astro:page-load", function () {
        // Animate hero highlight
        const highlight = document.querySelector(".hero-highlight");
        if (highlight) {
            setTimeout(() => highlight.classList.add("animated"), 200);
        }

        // Scroll animations
        const scrollElements = document.querySelectorAll('.scroll-animate');
        
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -50px 0px'
        };
        
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting && !entry.target.classList.contains('animated')) {
                    entry.target.classList.add('animated');
                }
            });
        }, observerOptions);
        
        scrollElements.forEach(element => {
            observer.observe(element);
        });

        // Smooth scrolling for anchor links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e: Event) {
                e.preventDefault();
                const href = (anchor as HTMLAnchorElement).getAttribute('href');
                if (href) {
                    const target = document.querySelector(href);
                    if (target) {
                        target.scrollIntoView({
                            behavior: 'smooth',
                            block: 'start'
                        });
                    }
                }
            });
        });

        // Demo form handling
        const demoForm = document.getElementById('demo-request-form') as HTMLFormElement;
        if (demoForm) {
            demoForm.addEventListener('submit', function(e: Event) {
                e.preventDefault();
                
                // Get form data
                const formData = new FormData(this as HTMLFormElement);
                const data = Object.fromEntries(formData);
                
                // Here you would typically send the data to your backend
                console.log('Demo request:', data);
                
                // Show success message (you can replace this with actual form submission)
                alert('Thank you! We\'ll be in touch soon to schedule your demo.');
                
                // Reset form
                (this as HTMLFormElement).reset();
            });
        }
    });
</script>
