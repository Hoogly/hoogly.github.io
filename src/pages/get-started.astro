---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Get Started | Hoogly" description="Sign up to get started with Hoogly." type="website">
  <style>
    body, html {
      background: #faf6f4 !important;
    }
    
    /* Override the global body gradient for this page */
    body {
      background: #faf6f4 !important;
    }
    
    /* Fallback to hide header on get-started page in case conditional rendering fails */
    header, .header, #navbar {
      display: none !important;
    }

    .get-started-bg {
      min-height: 100vh;
      background: #faf6f4;
      display: flex;
      align-items: flex-start;
      justify-content: center;
    }
    
    /* Override any container styling that might add purple background */
    .container {
      background: transparent !important;
    }
    
    /* Override main element styling */
    main {
      background: transparent !important;
    }
    .get-started-form-container {
      background: #fff;
      border-radius: 1.5rem;
      box-shadow: 0 4px 24px 0 rgba(0,0,0,0.04);
      padding: 2.5rem 2.5rem 3.5rem 2.5rem;
      max-width: 500px;
      width: 100%;
      margin: 8rem auto 4rem auto;
      display: flex;
      flex-direction: column;
      align-items: center;
    }
    .anonymous-ai-headline {
      font-family: 'Outfit', sans-serif;
      font-size: 2rem; /* Decrease from 2.5rem to 2rem for desktop */
      font-weight: 400;
      color: #23221F;
      text-align: center;
      margin-bottom: 2.5rem;
      line-height: 1.13;
      letter-spacing: -0.01em;
    }
    .hero-highlight {
      position: relative;
      color: #23221F;
      font-weight: 600;
      padding: 0.08em 0.5em;
      border-radius: 2em;
      display: inline-block;
      overflow: visible;
      background: none;
      z-index: 1;
      vertical-align: middle;
      font-size: inherit;
      line-height: inherit;
      font-family: inherit;
      transition: color 0.2s;
      word-wrap: break-word;
      hyphens: auto;
    }

    .hero-highlight::before {
      content: '';
      position: absolute;
      left: 0;
      top: 0;
      height: 100%;
      width: 0%;
      background: rgba(255, 125, 97, 0.5);
      border-radius: 2em;
      z-index: 0;
      transition: width 1.2s cubic-bezier(0.77, 0, 0.175, 1);
    }

    .hero-highlight.animated::before {
      width: 100%;
    }

    .hero-highlight.animated {
      color: #23221F;
    }

    .hero-highlight-text {
      position: relative;
      z-index: 1;
    }

    @media (max-width: 600px) {
      .get-started-form-container {
        padding: 1.5rem 0.5rem 2.5rem 0.5rem;
        max-width: 98vw;
      }
      .anonymous-ai-headline {
        font-size: 1.5rem;
      }
    }

    /* Style Pipedrive's form */
    .pipedriveWebForms {
      width: 100% !important;
      max-width: 370px !important;
      margin: 0 auto !important;
    }

    /* Style the iframe */
    .pipedriveWebForms iframe {
      width: 100% !important;
      border: none !important;
      background: transparent !important;
      min-height: 400px !important;
    }

    /* Hide Pipedrive's form title since we have our own */
    .pipedriveWebForms iframe body [data-testid="webform-title"] {
      display: none !important;
    }

    /* Style form inputs */
    .pipedriveWebForms iframe body input[type="text"],
    .pipedriveWebForms iframe body input[type="email"] {
      width: 100% !important;
      max-width: 370px !important;
      padding: 1.1rem 1.2rem !important;
      border-radius: 0.7rem !important;
      border: 1px solid #e0e0e0 !important;
      background: #fff !important;
      font-size: 1.15rem !important;
      margin-bottom: 0.5rem !important;
      font-family: 'Outfit', sans-serif !important;
      color: #23221F !important;
      box-shadow: 0 2px 8px 0 rgba(0,0,0,0.04) !important;
    }

    /* Style form labels */
    .pipedriveWebForms iframe body label {
      font-family: 'Outfit', sans-serif !important;
      font-size: 1rem !important;
      color: #23221F !important;
      margin-bottom: 0.5rem !important;
      font-weight: 400 !important;
    }

    /* Style submit button */
    .pipedriveWebForms iframe body button[type="submit"] {
      background: #ff8661 !important;
      color: #fff !important;
      font-family: 'Outfit', sans-serif !important;
      font-size: 1.1rem !important;
      font-weight: 500 !important;
      border: none !important;
      border-radius: 2rem !important;
      padding: 1.1rem 2.8rem !important;
      margin-top: 0.5rem !important;
      cursor: pointer !important;
      transition: background 0.2s !important;
      box-shadow: 0 2px 8px 0 rgba(0,0,0,0.03) !important;
    }

    .pipedriveWebForms iframe body button[type="submit"]:hover {
      background: #ff6a3d !important;
    }

    /* Style error messages */
    .pipedriveWebForms iframe body .error-message {
      color: #dc3545 !important;
      font-size: 0.9rem !important;
      margin-top: 0.5rem !important;
      font-family: 'Outfit', sans-serif !important;
    }
  </style>
  <div class="get-started-bg">
    <div class="get-started-form-container">
      <div class="anonymous-ai-headline">Let's <span class="hero-highlight"><span class="hero-highlight-text">Get Started</span></span></div>
      
      <!-- Pipedrive form wrapped in our styling -->
      <div class="pipedriveWebForms" data-pd-webforms="https://webforms.pipedrive.com/f/6ckSJfrxkGWnofmf9fDM6OhIecUWUcEvcNjg3R2t0ANapg7vg6A059q6nrqAqTnpHJ"></div>
    </div>
  </div>
  <script>
    document.addEventListener("astro:page-load", function () {
      // Load Pipedrive form script
      const script = document.createElement('script');
      script.src = 'https://webforms.pipedrive.com/f/loader';
      script.async = true;
      document.body.appendChild(script);

      // Trigger highlight animation for regular form page
      const highlight = document.querySelector('.hero-highlight');
      if (highlight) {
        setTimeout(() => highlight.classList.add('animated'), 200);
      }
    });
  </script>
</Layout> 