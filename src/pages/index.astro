---
import Layout from '../layouts/Layout.astro';
---

<Layout 
	title="Hoogly | Reinventing Employee Engagement"
	description="AI-powered employee engagement platform that's 10X better than surveys. Transform your workplace culture with Venn AI."
	type="website"
>
	<!-- Hero Section -->
	<section class="hero hero-fullscreen">
		<div class="container">
			<div class="hero-content">
				<div class="hero-text">
					<div class="hero-title" style="text-align: center; margin-bottom: 0.2em; font-family: 'Outfit', sans-serif;">
						<div class="hero-title-align" style="display: block;">
							<span class="hero-title-text">Tired of </span><span class="hero-highlight"><span class="hero-highlight-text">'meh' survey results?</span></span>
						</div>
						<div class="hero-title-second-line">
							So are your employees.
						</div>
					</div>
					<div class="hero-subtitle" style="font-family: 'Outfit', sans-serif; font-weight: 400; text-align: center; margin-bottom: 2.5em; padding-top: 1rem; line-height: 30px;">
						Our AI Culture coach chats anonymously with each<br>
						employee to uncover <b style="font-weight: 600;">honest, actionable insights that are 10x better</b>
					</div>
					<div style="display: flex; justify-content: center;">
						<a
							href="https://calendly.com/hoogly/30min"
							target="_blank"
							rel="noopener noreferrer"
							class="btn btn-accent"
							style="font-size: 1.5rem; padding: 1rem 2.5rem; border-radius: 71px;"
						>
							Start for Free
						</a>
					</div>
				</div>
			</div>
			<div class="hero-flip-image" style="display: flex; justify-content: center; align-items: flex-end; margin-top: 4.5rem;">
				<img id="heroFlipImage" src="/images/new%20dash.png" alt="Dashboard Hero" style="max-width: 100%; height: auto; width: 1162.33px; min-width: 0; border-radius: 18px; box-shadow: 0 8px 32px rgba(44, 41, 80, 0.12);" />
			</div>
		</div>
	</section>

	<!-- Anonymous 1:1 Conversations Section -->
	<section class="anonymous-ai-section section-gradient">
		<div class="container">
			<div class="anonymous-ai-content">
				<img src="/images/Section 1.png" alt="Section 1" class="anonymous-ai-image animate-in" style="width: 420px; max-width: 100%;" />
				<div class="anonymous-ai-text">
					<h2 class="anonymous-ai-headline" style="font-family: 'Outfit', sans-serif; font-weight: 400;"><span class="hero-highlight"><span class="hero-highlight-text">Anonymous</span></span><span> 1:1 conversations that uncover what surveys miss</span></h2>
					<p class="anonymous-ai-subheadline" style="font-family: 'Outfit', sans-serif; font-weight: 400;">
						Surveys give you answers people think they're supposed to say. Venn, our AI culture coach, chats privately with every employee—so they can speak freely and be truly heard. You don't just collect data. You unlock trust.
					</p>
				</div>
			</div>
		</div>
	</section>

	<!-- Spot the Risks Section -->
	<section class="anonymous-ai-section anonymous-ai-section-tight">
		<div class="container">
			<div class="anonymous-ai-content">
				<div class="anonymous-ai-text">
					<h2 class="anonymous-ai-headline" style="font-family: 'Outfit', sans-serif; font-weight: 400;"><span class="hero-highlight"><span class="hero-highlight-text">Emotions are messy.</span></span> We make sense of them — at scale.</h2>
					<p class="anonymous-ai-subheadline" style="font-family: 'Outfit', sans-serif; font-weight: 400;">
						Venn doesn't just listen—he reads between the lines. By analysing tone, patterns, and emerging themes across every conversation, our AI surfaces what's really going on beneath the surface. So instead of noise, you get clarity on what to act on now—and what to watch next.
					</p>
				</div>
				<img src="/images/Section 2.png" alt="Section 2" class="anonymous-ai-image animate-in" style="width: 420px; max-width: 100%;" />
			</div>
		</div>
	</section>

	<!-- Spot the Risks Section -->
	<section class="anonymous-ai-section anonymous-ai-section-tight section-gradient">
		<div class="container">
			<div class="anonymous-ai-content">
				<img src="/images/Section 3.png" alt="Section 3" class="anonymous-ai-image animate-in" style="width: 420px; max-width: 100%;" />
				<div class="anonymous-ai-text">
					<h2 class="anonymous-ai-headline" style="font-family: 'Outfit', sans-serif; font-weight: 400;">Turning employee feedback into <span class="hero-highlight"><span class="hero-highlight-text">Action</span></span></h2>
					<p class="anonymous-ai-subheadline" style="font-family: 'Outfit', sans-serif; font-weight: 400;">
						Venn transforms honest feedback into practical next steps for every employee, manager, and HR leader. Whether it's better 1:1s, clearer goals, or team rituals, each plan is tailored to what your people actually need. Because meaningful change doesn't start with dashboards—it starts with action.
					</p>
				</div>
			</div>
		</div>
	</section>

	<!-- Spot the Risks Section -->
	<section class="anonymous-ai-section anonymous-ai-section-tight">
		<div class="container">
			<div class="anonymous-ai-content">
				<div class="anonymous-ai-text">
					<h2 class="anonymous-ai-headline" style="font-family: 'Outfit', sans-serif; font-weight: 400;"><span class="hero-highlight"><span class="hero-highlight-text">AI agents</span></span> that help you follow through, without falling behind</h2>
					<p class="anonymous-ai-subheadline" style="font-family: 'Outfit', sans-serif; font-weight: 400;">
						Great plans often stall in the busyness of daily work. Hoogly's AI agents keep things moving—nudging the right people at the right time, booking check-ins, tracking progress, and celebrating wins. Whether you're deepening team rituals or reinforcing boundaries, you've got intelligent backup to make it stick.
					</p>
				</div>
				<img src="/images/Section 4.png" alt="Section 4" class="anonymous-ai-image animate-in" style="width: 420px; max-width: 100%;" />
			</div>
			<div class="integrations-subsection" style="margin-top: 4rem;">
				<h3 class="integrations-title animate-in" style="text-align: center; margin-bottom: 2.5rem; color: #2d2a58; font-weight: 400; font-family: 'Outfit', sans-serif; font-size: var(--font-size-lg);">Integrates with:</h3>
				<div class="integration-logos animate-in" style="display: flex; justify-content: center; align-items: center; gap: 4rem; flex-wrap: wrap;">
					<img src="/images/slack-logo.png" alt="Slack" style="height: 35px; filter: grayscale(1) opacity(0.7);" />
					<img src="/images/teams-logo.png" alt="Microsoft Teams" style="height: 35px; filter: grayscale(1) opacity(0.7);" />
					<img src="/images/hibob-logo.png" alt="HiBob" style="height: 35px; filter: grayscale(1) opacity(0.7);" />
					<img src="/images/bamboohr-logo.png" alt="BambooHR" style="height: 35px; filter: grayscale(1) opacity(0.7);" />
					<img src="/images/workday-logo.png" alt="Workday" style="height: 35px; filter: grayscale(1) opacity(0.7);" />
				</div>
			</div>
		</div>
	</section>

	<!-- Final CTA Section -->
	<section class="final-cta-section" style="background: #F7F3F0; text-align: center; padding: var(--space-20) 0;">
		<div class="container">
			<h2 class="final-cta-title" style="font-family: 'Outfit', sans-serif; font-weight: 400; font-size: var(--font-size-4xl); margin-bottom: 1rem;"><span class="hero-highlight"><span class="hero-highlight-text">Build Culture,</span></span> don't just measure it.‍</h2>
			<p style="font-size: var(--font-size-xl); color: var(--color-neutral-600); max-width: 600px; margin: 0 auto var(--space-8) auto; font-family: 'Outfit', sans-serif; font-weight: 400;">
				Run an AI experiment. Try Hoogly alongside your next survey.<br>
				Once you experience this new way of listening, the old way won't cut it anymore.
			</p>
			<a href="https://calendly.com/hoogly/30min" target="_blank" rel="noopener noreferrer" class="btn btn-accent" style="font-size: 1.25rem; padding: 1rem 2rem; border-radius: 71px;">Start for Free</a>
		</div>
	</section>
</Layout>

<style>
.animate-in {
  opacity: 0;
  transform: translateY(40px);
  transition: opacity 1.2s cubic-bezier(0.77,0,0.175,1), transform 1.2s cubic-bezier(0.77,0,0.175,1);
}
.animate-in.visible {
  opacity: 1;
  transform: translateY(0);
}
</style>

<script>
window.addEventListener('DOMContentLoaded', function() {
  const highlight = document.querySelector('.hero-highlight');
  if (highlight) {
    setTimeout(() => highlight.classList.add('animated'), 200);
  }

  // Universal observer: animate every .hero-highlight on scroll into view
  document.querySelectorAll('.hero-highlight').forEach(highlight => {
    const observer = new window.IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          highlight.classList.add('animated');
          observer.disconnect();
        }
      });
    }, { threshold: 0.3 });
    observer.observe(highlight);
  });

  // Scroll-synced flip and fade animation with early start
  const heroFlipImage = document.getElementById('heroFlipImage');
  const offset = 100; // px before top of viewport
  function handleFlipScroll() {
    if (!heroFlipImage) return;
    const rect = heroFlipImage.getBoundingClientRect();
    const imageHeight = heroFlipImage.offsetHeight;
    if (rect.top >= offset) {
      heroFlipImage.style.transform = 'rotateX(0deg) scale(0.95)';
      heroFlipImage.style.opacity = '1';
    } else if (rect.top <= offset - imageHeight) {
      heroFlipImage.style.transform = 'rotateX(90deg) scale(0.95)';
      heroFlipImage.style.opacity = '0';
    } else {
      // Interpolate angle and opacity from 0deg/1 to 90deg/0 as the image scrolls out
      const ratio = Math.min(1, Math.max(0, (offset - rect.top) / imageHeight));
      const angle = ratio * 90;
      const opacity = 1 - ratio;
      heroFlipImage.style.transform = `rotateX(${angle}deg) scale(0.95)`;
      heroFlipImage.style.opacity = opacity.toString();
    }
  }
  window.addEventListener('scroll', handleFlipScroll);
  handleFlipScroll(); // Run once on load

  // Animate all section images in on viewport
  document.querySelectorAll('.anonymous-ai-image.animate-in').forEach(img => {
    const observer = new window.IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          img.classList.add('visible');
          observer.disconnect();
        }
      });
    }, { threshold: 0.3 });
    observer.observe(img);
  });

  // Animate all section images and integrations in on viewport
  document.querySelectorAll('.anonymous-ai-image.animate-in, .integrations-title.animate-in, .integration-logos.animate-in').forEach(el => {
    const observer = new window.IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          el.classList.add('visible');
          observer.disconnect();
        }
      });
    }, { threshold: 0.3 });
    observer.observe(el);
  });
});
</script>
